[] Na parte "smart" colocar uma chave para cada editor. Ex:
   atom.enable = true
   vscode.enable = true
   sublime.enable = true

[] Usar forma interativa para comando unlink, link, pull, restore

[x] Comando "dotctrl list" deve listar arquivos do repo e não da lista elements = [].

[x] Colocar opção de "dotctrl unlink --file=.config/foo.conf". Deve remover o link da HOME.

[] Colocar opção de "dotctrl remove --file=.config/foo.conf". Deve remover o link da HOME
   e remover o elemento da lista "elements = []" e restaurar o elementos para diretorio original.

[] Colocar opção de "dotctrl restore --file=.config/foo.conf". Deve remover o link da HOME
   e restaurar o elmentos para diretorio original.

[] Opção dotctrl element --add=.config/foo.txt adiciona no elements = []
